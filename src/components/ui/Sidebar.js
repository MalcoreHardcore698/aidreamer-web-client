/*
 * COMPONENT: Sidebar
 * 
 * MISSION: ...
 *
**/

import React from 'react'
import '../styles/Sidebar.css'

export default ({ options }) => {
    const {

    } = options

    const classes = [
        'ui-sidebar'
    ]

    return (
        <div className={classes.join(' ')}>
            {/* Quiet... */}
        </div>
    )
}